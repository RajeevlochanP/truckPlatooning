/**
 * Application Configuration
 */
export const config = {
  port: process.env.PORT || 3000,
  appName: "Truck Platooning Verifiable Computation Chain",
  thresholdPathLength: 2,
};

export default config;
